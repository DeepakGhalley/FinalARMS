CREATE TABLE [dbo].[mstDisposalType]
(
	[disposalTypeId] INT NOT NULL PRIMARY KEY IDENTITY,
	[disposalType] varchar(250) NOT NULL,
    [disposalTypeDescription] varchar(300),
	[isActive] BIT NOT NULL, 
    [createdBy] INT NOT NULL, 
    [createdOn] DATETIME NOT NULL, 
    [modifiedBy] INT NULL, 
    [modifiedOn] DATETIME NULL
)
